
// eslint-disable-next-line react/prop-types
function Total({ inputData, tip}) {
    

    
    
  

  return (
      <div>
          <h2>You pay Ksh {inputData + tip} (Ksh {inputData} + {tip} tip)</h2>
    </div>
  )
}

export default Total